package sart.player.web

import sart.dart.*

/** A DOM event (package:web). The typed param of an `addEventListener`
 *  handler (`(Event e) => …`). */
@native
@DartImport("package:web/web.dart")
@DartPackage("web", "^1.1.0")
class Event extends DartObject
